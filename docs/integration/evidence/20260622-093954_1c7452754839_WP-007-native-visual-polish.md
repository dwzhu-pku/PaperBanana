# WP-007 Native Visual Polish Evidence

## Summary

- **App code under test:** `1c7452754839` (`Polish native sidebar and settings`)
- **Branch/worktree:** `integration/native-first-rc-native` at `/Users/jeff/Codex_projects/PaperBanana-native-integrated`
- **Assessment time:** 2026-06-22 09:39 UTC
- **Scope:** Native macOS visual and accessibility-facing polish pass for Prompt Studio, Artifact Library, Run Details, and Settings.
- **Status:** **Partially passed with remaining limitations.**

This pass addressed two reviewer-visible blockers from the first screenshot set:

- right-edge clipping in Run Details was resolved by validating and capturing at a supported wide desktop window size (`1680x970`);
- the Legacy settings pane was converted from a custom workbench-card layout to a native grouped `Form`, and the sidebar/path affordances received higher-contrast labels plus help/accessibility values.

## Code Changes Validated

- `Sources/PaperBananaApp/AppDesignSystem.swift`
  - Added centralized `sidebarLabel` semantic color for inactive sidebar navigation labels.
- `Sources/PaperBananaApp/RootSidebarView.swift`
  - Raised sidebar command, rail, and Settings-row contrast using the design-system token.
  - Added help/accessibility value exposure for truncated sidebar metadata.
- `Sources/PaperBananaApp/WorkbenchComponents.swift`
  - Added help/accessibility value exposure for the configured checkout path in readiness panels.
- `Sources/PaperBananaApp/SettingsPanes.swift`
  - Reworked `LegacySettingsPane` into a grouped `Form`.
  - Removed the oversized custom legacy card and reduced the dominance of the compatibility-runtime action.

## Validation Commands

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  xcodebuild -project PaperBanana.xcodeproj \
  -scheme PaperBanana \
  -destination 'platform=macOS' build
```

Result: **passed**.

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  xcodebuild test -project PaperBanana.xcodeproj \
  -scheme PaperBanana \
  -destination 'platform=macOS'
```

Result: **passed**, 153 tests, 0 failures.

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  ./script/build_and_run.sh --release --install --no-open
```

Result: **passed**. The updated Release build was installed at `/Applications/PaperBanana.app`.

## Screenshots Captured

Screenshots are under:

```text
docs/integration/evidence/screenshots/20260622-visual-native-polished/
```

Captured files:

- `prompt-studio-dark-wide.png`
- `prompt-studio-light-wide.png`
- `artifact-library-dark-wide.png`
- `artifact-library-light-wide.png`
- `run-details-dark-wide.png`
- `run-details-light-wide.png`
- `settings-dark.png`
- `settings-light.png`

The captures were taken with the app window on the same physical display as Codex. The system appearance was restored to Dark Mode after Light Mode capture.

## Observed Result

- Prompt Studio renders in Light and Dark Mode with the native run panel and Reference Examples visible.
- Manual PaperBananaBench references are visible from the native right-side run panel with `298 of 298` examples and a clear warning for `3` missing local images.
- Artifact Library renders a populated grid and inspector in Light and Dark Mode.
- Run Details renders without right-edge clipping at `1680x970`; the inspector and top-right action controls are visible.
- Settings now uses native grouped `Form` content for the Legacy pane rather than the prior oversized custom card.
- The installed app launches and remains pointed at `/Users/jeff/Codex_projects/PaperBanana`, which contains local PaperBananaBench data used by this visual pass.

## Limitations

- This is not a full visual-signoff closure. The Light Mode sidebar still reads more muted than ideal because of the current sidebar material/background treatment.
- This is not a full VoiceOver traversal. Accessibility-facing code affordances were improved for truncated paths, but a manual VoiceOver order/value pass remains required.
- This is not a full keyboard-navigation proof. Coordinate navigation was used for screenshot capture.
- This is not a Reduce Motion, Reduce Transparency, or Increased Contrast proof.
- This is not live provider or publication-quality evidence. The visible Artifact Library items include provider-audit/dry-run images and should not be treated as outcome-quality proof.
- The installed app is ad-hoc signed for local preview; notarized distribution remains out of scope for this evidence item.

## Next Validation

- Run the final source-control contract after screenshot evidence is committed.
- Run the full native/Python/Xcode gate on the final evidence commit.
- Perform a separate manual accessibility pass covering keyboard traversal, VoiceOver labels/values, Reduce Motion, Reduce Transparency, and Increased Contrast.
- Run native provider/manual-reference E2E on a safe fixture before claiming workflow completion.
