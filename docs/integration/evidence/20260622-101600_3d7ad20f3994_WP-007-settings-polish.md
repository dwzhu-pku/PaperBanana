# WP-007 Settings Polish Evidence

## Summary

- **App code under test:** `3d7ad20f3994` (`Polish native Settings forms`)
- **Branch/worktree:** `integration/native-first-rc-native` at `/Users/jeff/Codex_projects/PaperBanana-native-integrated`
- **Assessment time:** 2026-06-22 10:16 UTC
- **Scope:** Bounded native macOS Settings polish after visual/accessibility review.
- **Status:** **Partially passed with remaining limitations.**

This pass does not claim full WP-007, full Settings visual signoff, or full VoiceOver/keyboard signoff. It addresses the Settings-specific issues raised by read-only review lanes and adds screenshot evidence for the native Preferences tabs in both Light and Dark Mode.

## Parallel Review Inputs

- A read-only evidence/doc lane identified the required manifest row, evidence-file boundary, validation commands, and overclaiming risks.
- A macOS design critic graded the prior Settings screenshots **B-** as partial evidence only. The critic asked to avoid full signoff language, noted clipped Legacy diagnostics, requested better path affordances, and recommended preserving the native `TabView`, grouped `Form`, max-width content, and Settings-native readiness rows.

## Code Changes Validated

- `Sources/PaperBananaApp/SettingsView.swift`
  - Retains native tabbed Settings while adding typed pane selection and named pane landmarks.
  - Centers Settings content with a bounded max width.
  - Increases the default Settings height enough for the Legacy diagnostics section to be visible in the default screenshot state.
  - Adds a named Settings window accessibility landmark.
- `Sources/PaperBananaApp/SettingsPanes.swift`
  - Replaces the workbench readiness card inside Workspace Settings with native Settings form rows.
  - Adds help/accessibility values for the Workspace checkout path and secret-store path.
  - Uses responsive `ViewThatFits` action rows for provider, legacy, and diagnostics actions.
  - Adds stable Settings control identifiers for screenshot-state and accessibility review.
  - Uses Settings-local status pills for provider and diagnostics states.
- `tests/PaperBananaTests/NoCredentialServicesRegressionTests.swift`
  - Extends the native source-control/design regression contract for the bounded Settings layout, readiness-row replacement, responsive actions, path help, and named controls.

## Screenshot Evidence

Captured from `/Applications/PaperBanana.app` after:

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  ./script/build_and_run.sh --release --install --no-open
```

Screenshot set:

```text
docs/integration/evidence/screenshots/20260622-settings-polish/settings-light-workspace.png
docs/integration/evidence/screenshots/20260622-settings-polish/settings-dark-workspace.png
docs/integration/evidence/screenshots/20260622-settings-polish/settings-light-providers.png
docs/integration/evidence/screenshots/20260622-settings-polish/settings-dark-providers.png
docs/integration/evidence/screenshots/20260622-settings-polish/settings-light-legacy.png
docs/integration/evidence/screenshots/20260622-settings-polish/settings-dark-legacy.png
```

All six screenshots are `1892 x 1388` PNG files. The refreshed Legacy captures show the full diagnostics warning set without the clipped OpenRouter row seen in the earlier draft capture.

## Validation Commands

```bash
git diff --check --cached
```

Result: **passed**.

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  ./script/check_xcode_project_drift.sh
```

Result: **passed**; `PaperBanana.xcodeproj` matches `project.yml`.

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  ./script/check_native_source_control_contract.sh
```

Result before commit with the final staged screenshots: **passed**.

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  xcrun xcodebuild test \
  -project PaperBanana.xcodeproj \
  -scheme PaperBanana \
  -destination 'platform=macOS' \
  -only-testing:PaperBananaTests/NoCredentialServicesRegressionTests
```

Result: **passed**, 15 tests, 0 failures.

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  xcrun xcodebuild test \
  -project PaperBanana.xcodeproj \
  -scheme PaperBanana \
  -destination 'platform=macOS'
```

Result: **passed**, 154 tests, 0 failures.

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  ./script/build_and_run.sh --release --install --no-open
```

Result: **passed**; `PaperBanana` was installed at `/Applications/PaperBanana.app`.

## Material Warnings

- Xcode test runs still emit AppIntents/linkd and Core Spotlight donation warnings in this local test environment. Tests completed successfully.
- This pass used source-contract tests and screenshot inspection. It did not add a UI-test target.
- The screenshot set covers default wide Settings windows only, not narrow windows or focus/hover/disabled variants.

## Limitations

- Full VoiceOver traversal remains required across Settings, reference rows, artifact grid context menus, disabled states, and preflight sheets.
- Full keyboard navigation evidence remains required, including the native Table focus naming issue noted in the previous accessibility evidence.
- Reduce Motion, Reduce Transparency, Increased Contrast, Increased Text Size, hover/focus, and inactive-window visual review remain open.
- Settings severity color and status-pill opacity still require increased-contrast review before full visual signoff.
- Workspace path and readiness row density are improved but still need narrow-width and larger-text review.
- No live provider, hosted deployment, rollback, or publication-quality evidence is provided by this pass.

## Next Validation

- Run a full manual keyboard and VoiceOver traversal of Settings and the remaining native surfaces.
- Capture narrow-width, focused, inactive-window, Increased Contrast, Increased Text Size, Reduce Transparency, and Reduce Motion Settings states.
- Decide whether readiness row severity tint should be reduced to icon-only tint for full Settings signoff.
- Run the full native/Python/Xcode gate on the final evidence commit.
