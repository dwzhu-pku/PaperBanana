# WP-108 Evaluation Inventory Evidence

## Summary

- **Source snapshot inspected:** `ee9a206472e55c35e80300f3514a76871653066f`
- **Branch/worktree:** `integration/native-first-rc-native` at `/Users/jeff/Codex_projects/PaperBanana-native-integrated`
- **Assessment time:** 2026-06-22 12:01 EDT
- **Scope:** No-live inventory of evaluation, benchmark, ground-truth, and quality-claim surfaces relevant to WP-108/G-13.
- **Status:** **Passed with limitations; no runnable no-live WP-108 benchmark command exists.**

The repository contains evaluation-adjacent code and a Streamlit result viewer,
but this pass did not find a standalone benchmark runner, frozen manifest,
acceptance threshold, report schema, reviewer rubric, or CI gate that can
validate publication-quality outcomes without provider calls.

This evidence strengthens the release boundary: PaperBanana must not be
described as publication-quality or quality-benchmark-validated until WP-108 is
implemented with a frozen rubric/manifest and an approved no-live or bounded
live validation path.

## Commands And Results

Repository inventory:

```bash
rg -n "evaluation|evaluate|benchmark|ground.?truth|metric|quality|WP-108|publication-quality|publication quality" \
  README.md docs scripts tests utils visualize prompts main.py .github \
  --glob '!data/**'
```

Result: evaluation and quality references exist in README/docs/source/tests, but
no ready WP-108 benchmark runner or quality gate was found.

Import-spec probe for the Streamlit referenced-evaluation viewer:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. \
/Users/jeff/Codex_projects/PaperBanana/.venv/bin/python - <<'PY'
import importlib.util
for name in ['prompts.plot_eval_prompts', 'prompts.plots_eval_prompts']:
    spec = importlib.util.find_spec(name)
    print(f'{name}: {"FOUND" if spec else "MISSING"}')
PY
```

Result:

```text
prompts.plot_eval_prompts: FOUND
prompts.plots_eval_prompts: MISSING
```

Safe collection-only validation for adjacent non-live contracts:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. \
/Users/jeff/Codex_projects/PaperBanana/.venv/bin/python \
  -m pytest --collect-only -q -p no:cacheprovider \
  tests/test_docs_contract.py tests/test_legacy_ui_result_keys.py
```

Result:

```text
20 tests collected in 0.02s
```

## Observed Evaluation Surfaces

| Surface | Evidence | Interpretation |
|---|---|---|
| README visualization command | `README.md` documents `streamlit run visualize/show_referenced_eval.py` for viewing evaluation results. | This is a viewer for existing result files, not a release-quality benchmark runner. |
| README quality/evaluation claims | README describes publication-quality output and a built-in evaluation framework. | These are product claims or capability descriptions; they are not WP-108 completion evidence without a frozen run and report. |
| Referenced-evaluation viewer | `visualize/show_referenced_eval.py` accepts a results JSONL path, computes aggregate display metrics, and offers a live re-run button. | The viewer can inspect existing outputs, but it is not safe no-live validation because live re-run calls provider-backed evaluation. |
| Viewer plot hot-reload import | `visualize/show_referenced_eval.py` imports `prompts.plots_eval_prompts`, while the repository contains `prompts/plot_eval_prompts.py`. | The live plot debug path appears broken until the import is corrected and tested. |
| Evaluation toolkit | `utils/eval_toolkits.py` defines diagram/plot prompt maps, four comparison dimensions, ground-truth image resolution from `path_to_gt_image`, and an overall result calculation. | This is useful implementation substrate, but it depends on provider-backed model calls for scoring. |
| Pipeline evaluation hook | `utils/paperviz_processor.py` invokes evaluation when configured to do so. | This is tied to generation/evaluation execution, not a no-live benchmark. |
| CI and contract tests | Current CI/docs contracts keep WP-108 open and do not run benchmark scoring. | CI does not validate publication quality. |

## Material Warnings

- No checked-in `data/` benchmark directory exists in this integration checkout.
  Local PaperBananaBench data remains an external prerequisite.
- The existing evaluator can call Gemini, OpenAI, or Claude helper routes. Any
  real scoring run therefore has cost, credential, quota, privacy, latency, and
  nondeterminism risks unless bounded and explicitly approved.
- The Streamlit viewer's live plot debug path appears to reference a missing
  module name: `prompts.plots_eval_prompts`.
- Existing evaluator prompts are not an acceptance threshold. WP-108 still
  needs a frozen manifest, pass/fail rubric, reviewer policy, output-retention
  policy, and report schema before it can support release-quality claims.

## Exclusions

- No live provider call was made.
- No benchmark image or private scientific input was copied.
- No Streamlit app, Gradio app, native app, or browser was launched.
- No code was changed as part of this inventory.

## Smallest Decisive Next Step

Create a no-live WP-108 benchmark harness contract before any live scoring:

- define a small fixed manifest of non-private diagram and plot cases;
- define a JSON report schema and pass/fail threshold;
- support a dry-run/fixture evaluator mode that exercises manifest loading,
  ground-truth path resolution, generated-output discovery, and report writing
  without provider calls;
- keep live provider scoring behind explicit credentials/spend approval; and
- add a focused CI-safe contract test for the no-live harness.

Only after that harness passes should the project run a bounded live or
reviewer-scored quality benchmark and update the release claim boundary.
