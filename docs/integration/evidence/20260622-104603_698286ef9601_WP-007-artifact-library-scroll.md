# WP-007 Artifact Library Scroll Screenshot Evidence

## Summary

- **App code under test:** `698286ef9601` (`Route Artifact Library through native intent bridge`)
- **Branch/worktree:** `integration/native-first-rc-native` at `/Users/jeff/Codex_projects/PaperBanana-native-integrated`
- **Assessment time:** 2026-06-22 10:46 UTC
- **Scope:** Artifact Library inspector top/bottom scroll evidence in Light and Dark Mode.
- **Status:** **Passed with limitations.**

This pass closes the specific Artifact Library lower-inspector screenshot gap from
the WP-007 evidence manifest. It does not claim full WP-007 visual signoff,
full keyboard/VoiceOver traversal, live provider validation, hosted validation,
rollback validation, or publication-quality evidence.

## Code Change Validated

- `Sources/PaperBananaApp/PaperBananaModels.swift`
  - Adds `PaperBananaIntentDestination.artifactLibrary`.
- `Sources/PaperBananaApp/RootView.swift`
  - Routes the native intent bridge to the Artifact Library sidebar destination.
- `tests/PaperBananaTests/NativeImageGenerationStoreTests.swift`
  - Verifies the intent bridge can consume the Artifact Library destination once.

This gives screenshot and App Intent automation a deterministic native entry
point into Artifact Library instead of relying on sidebar coordinates.

## Screenshot Evidence

Screenshots are under:

```text
docs/integration/evidence/screenshots/20260622-artifact-library-scroll/
```

Captured files:

- `artifact-library-light-top.png`
- `artifact-library-light-bottom.png`
- `artifact-library-dark-top.png`
- `artifact-library-dark-bottom.png`

All four PNG files are `3360 x 1940` retina captures from a `1680 x 970`
PaperBanana window.

The captures show:

- Artifact Library selected through the native sidebar.
- The populated `/Users/jeff/Codex_projects/PaperBanana` checkout (`231 images`,
  `240 total artifacts`) rather than an empty fixture.
- Top inspector states with Preview and Artifact metadata visible.
- Bottom inspector states after setting the right-side inspector AX scrollbar
  from `0.0` to `1.0`, showing Linked Files and Local Assistant above the fixed
  action footer.
- Light and Dark Mode appearance parity for the same selected artifact.
- The fixed action footer in the wrapped/two-row action layout, with controls
  remaining visible after the inspector content reaches the bottom.

## Validation Commands

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  xcrun xcodebuild test \
  -project PaperBanana.xcodeproj \
  -scheme PaperBanana \
  -destination 'platform=macOS' \
  -only-testing:PaperBananaTests/NativeImageGenerationStoreTests/testIntentBridgeConsumesRequestedDestinationOnce
```

Result: **passed**, 1 test, 0 failures.

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  ./script/build_and_run.sh --release --install --no-open
```

Result: **passed**. The updated Release build was installed at
`/Applications/PaperBanana.app`.

```bash
defaults write local.paperbanana.gui settings.repoPath -string "/Users/jeff/Codex_projects/PaperBanana"
defaults write local.paperbanana.gui paperbanana.intent.destination -string artifactLibrary
open -n -b local.paperbanana.gui
```

Result: **passed**. PaperBanana opened directly into Artifact Library.

```bash
osascript -e 'tell application "System Events" to tell process "PaperBanana" to set position of window "PaperBanana" to {20, 65}' \
  -e 'tell application "System Events" to tell process "PaperBanana" to set size of window "PaperBanana" to {1680, 970}'
```

Result: **passed**. The window was positioned on the same display as Codex at
`1680 x 970`.

```bash
xcrun swift -e 'import CoreGraphics; import Foundation; let list = CGWindowListCopyWindowInfo([.optionOnScreenOnly], kCGNullWindowID) as? [[String: Any]] ?? []; for w in list { if (w[kCGWindowOwnerName as String] as? String) == "PaperBanana" { print("\(w[kCGWindowNumber as String] ?? "")\t\(w[kCGWindowName as String] ?? "")\t\(w[kCGWindowBounds as String] ?? "")") } }'
```

Result: **passed**. The captured window was `PaperBanana`, window id `16143`,
with bounds `{X = 20; Y = 65; Width = 1680; Height = 970}`.

```bash
screencapture -x -o -l 16143 docs/integration/evidence/screenshots/20260622-artifact-library-scroll/artifact-library-light-top.png
screencapture -x -o -l 16143 docs/integration/evidence/screenshots/20260622-artifact-library-scroll/artifact-library-light-bottom.png
screencapture -x -o -l 16143 docs/integration/evidence/screenshots/20260622-artifact-library-scroll/artifact-library-dark-top.png
screencapture -x -o -l 16143 docs/integration/evidence/screenshots/20260622-artifact-library-scroll/artifact-library-dark-bottom.png
```

Result: **passed**. `sips -g pixelWidth -g pixelHeight` reported `3360 x 1940`
for each screenshot.

## AX Scroll Evidence

The right-side inspector was identified through local AX enumeration:

```text
scroll area pos=941,207 size=680x726
scroll bar pos=1603,206 size=19x728
```

Setting the right inspector scrollbar produced:

```text
right scrollbar old=0.0 new=1.0 pos=1603,206 size=19x728
```

The Light and Dark bottom screenshots were captured after that AX scrollbar
change, proving the lower inspector content remains reachable above the fixed
footer.

## Material Warnings

- Peekaboo automation was unavailable in this environment (`Agent service not
  available`), so the pass used local AppleScript, CoreGraphics, AX, and
  `screencapture`.
- There is still no checked-in UI-test target or deterministic screenshot
  harness. This pass relies on local AX for the bottom-scroll state.
- These captures are visual reachability evidence only; they are not a full
  VoiceOver reading-order proof or keyboard-navigation proof.
- This pass does not include separate hover/focus/inactive-window/increased-text
  or reduced-transparency captures.

## Next Validation

- Run the full native/Xcode gate and source-control contract after this evidence
  is committed.
- Run full manual keyboard and VoiceOver traversal across Artifact Library,
  Run Details, Provider Run Ledger, Settings, reference rows, disabled states,
  and preflight sheets.
- Run Reduce Motion, Reduce Transparency, Increased Contrast, Increased Text
  Size, hover/focus, inactive-window, and remaining adaptive-state reviews before
  claiming full WP-007 visual/accessibility signoff.
