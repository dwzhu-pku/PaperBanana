# WP-105 Exact-Head Native/Python Gate Evidence

Timestamp: 2026-06-22 05:15:16 EDT
Integration branch: `integration/native-first-rc-native`
Integration worktree: `/Users/jeff/Codex_projects/PaperBanana-native-integrated`

## SHAs

| Item | SHA |
|---|---|
| Exact validation head | `d43a6c8a9556d61cebc92148919e62da51c95a07` |
| Previous native integrated candidate | `26adc4670944c7524c6b28ce087e76966cd9aa35` |
| Python/security base | `c9282cd2ddbd5c5cf4d30ffbd4400975b91dfdac` |
| Upstream baseline | `ddeb2a9a8cf6c8119dd29a97c1f1a7312d27dc7b` |

## Purpose

This run revalidated the native-integrated branch after the evidence manifest
and diff-hygiene cleanup were committed. The preceding gate evidence was tied
to `26adc4670944`; this evidence ties the local full gate to the current branch
head after `d43a6c8`.

## Command

```text
PYTHONDONTWRITEBYTECODE=1 \
PAPERBANANA_PYTHON=/Users/jeff/Codex_projects/PaperBanana/.venv/bin/python \
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
CODEX_XCODE27_BIN="$(command -v codex-xcode27)" \
  ./script/test_all.sh
```

## Result

Exit code: `0`

Summarized output:

```text
PaperBanana native source-control contract passed.
PaperBanana native Xcode contract passed.
PaperBanana Xcode 27 baseline guard passed.
Executed 153 Swift tests, with 0 failures.
88 passed in 7.93s
status=passed halted=False
```

Generated local proof artifact:

```text
.codex/xcode27/2026-06-22T09-15-16Z-proof.json
.codex/xcode27/2026-06-22T09-15-16Z-proof.md
```

Known non-failing diagnostics:

- `xcodebuild` emitted `IDERunDestination: Supported platforms for the
  buildables in the current scheme is empty.`
- Xcode metadata extraction warned that no AppIntents framework dependency was
  found.

## Additional Focused Checks

```text
git status --short --branch
git diff origin/main --check
git diff --check
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  ./script/check_native_source_control_contract.sh
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. \
  /Users/jeff/Codex_projects/PaperBanana/.venv/bin/python \
  -m pytest -q -p no:cacheprovider tests/test_ci_contract.py tests/test_docs_contract.py
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. \
  /Users/jeff/Codex_projects/PaperBanana/.venv/bin/python \
  -m unittest tests.test_app_credential_isolation
```

Results:

```text
d43a6c8a9556d61cebc92148919e62da51c95a07
PaperBanana native source-control contract passed.
7 passed in 0.02s
Ran 6 tests in 5.604s - OK
```

Static credential search was manually classified: `app.py` contains only
server-startup/status credential reads, and the remaining key strings are in
credential-isolation tests or status copy. No `Apply Keys`, `apply_keys`,
password textbox, UI-originated `os.environ[...]` assignment, or `os.putenv`
credential mutation remained in `app.py`.

## Limitations

- This is a local gate, not GitHub check-run evidence.
- The full Xcode 27 workflow still requires a configured self-hosted runner if
  that gate is selected for branch protection.
- Live provider, hosted deployment, visual/accessibility, benchmark, release
  install, and rollback gates remain open release evidence items.
