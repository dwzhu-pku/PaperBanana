# WP-005 / WP-105 Current-Head Full Gate Evidence

- **Evidence ID:** `EV-20260623-069`
- **Assessment time:** 2026-06-23 08:16 EDT / 2026-06-23T12:16Z
- **Commit under test:** `4f9c4683e52f50e7cbef4262b9a41c4d64ffb60d`
- **Branch:** `integration/native-first-rc-native`
- **Scope:** WP-005/T-034/T-035 and WP-105/T-017/T-018/T-019 gate portability and current-head local full native/Python/Xcode 27 validation.

## Summary

The current head fixes a local gate portability failure in `script/test_all.sh`.
Before the fix, a no-venv checkout fell through to `/usr/local/bin/python3`
(`Python 3.14.6`) and failed Python test collection because that interpreter did
not have the project dependencies. The script now resolves Python tests in this
order: explicit `PAPERBANANA_PYTHON`, local `.venv`, isolated `uv` with
`python3.12` plus `requirements.txt` and `pytest`, then bare `python3` as a last
fallback.

After committing that fix, the documented full gate command passed on exact
commit `4f9c4683e52f50e7cbef4262b9a41c4d64ffb60d`.

## Validation Record

| Validation | Command | Result | Interpretation |
|---|---|---|---|
| Baseline branch state | `git rev-parse HEAD` | `4f9c4683e52f50e7cbef4262b9a41c4d64ffb60d` | Evidence is tied to the committed gate-script fix, not an unstaged worktree. |
| Shell syntax | `bash -n script/test_all.sh` | Passed | The portability edit is syntactically valid Bash. |
| Diff whitespace | `git diff --check` | Passed | No whitespace errors in the script patch. |
| Documented full local gate | `DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer CODEX_XCODE27_BIN=/Users/jeff/.codex/bin/codex-xcode27 ./script/test_all.sh` | Passed | The documented gate now completes without a local `.venv` by using the isolated Python 3.12 `uv` fallback. |
| Native source-control contract | Included in `script/test_all.sh` | Passed | Durable native source/project files were staged or committed before validation. |
| Xcode 27 baseline guard | Included in `script/test_all.sh` | Passed | Host audit and project scan passed; project scan reported 0 errors and 3 warnings, matching the standing non-fatal scan profile. |
| Xcode tests | Included in `script/test_all.sh`; xcresult `/Users/jeff/Library/Developer/Xcode/DerivedData/PaperBanana-cqvqlzqnotvuwjgsxhcuiysgzunn/Logs/Test/Test-PaperBanana-2026.06.23_08-16-15--0400.xcresult` | Passed: 167 passed, 0 failed, 0 skipped on My Mac, macOS 27.0, arm64 | Current native app and native tests pass under Xcode 27 on the local Apple Silicon host. |
| Python tests | Included in `script/test_all.sh` through isolated `uv` fallback | Passed: 126 passed, 8 warnings | Python 3.12 regression suite passed from a clean isolated dependency environment. Warnings were existing `datetime.utcnow()` deprecations in `utils/provider_audit.py`. |
| Xcode 27 proof | Included in `script/test_all.sh`; `.codex/xcode27/2026-06-23T12-16-41Z-proof.md` | Passed: `status=passed halted=False` | `codex-xcode27 proof` passed on the same source root and scheme. |

## Remote Check Context

Immediately before the gate-script fix, pushed head
`9649730654f970e922c2767f2280afac5041f55d` passed the quick remote workflows:

- `Native Structural Checks` run `27986985078`, job `82830713239`, passed.
- `Python Tests` run `27986985058`, job `82830713164`, passed.

Attempting to dispatch `Native Xcode 27 Full Gate` for the integration branch
failed with GitHub HTTP 404 because `native-xcode27-full-gate.yml` is not present
on the repository default branch. That is a workflow-dispatch/default-branch
limitation, not a local Xcode test failure. The local full gate above is the
current proof for `4f9c468`.

## Material Warnings

- Xcode emitted non-fatal `linkd.autoShortcut`, Core Spotlight donation, and
  TextRecognition model bundle warnings seen in earlier runs. They did not fail
  tests or `codex-xcode27 proof`.
- The gate uses local Xcode 27 and local `codex-xcode27`. It does not replace a
  successful remote self-hosted Xcode 27 workflow run if that remains a required
  release or branch-protection check.
- This evidence does not run live providers, Hugging Face hosted deployment,
  reviewer quality scoring, full manual VoiceOver traversal, or final release
  rollback/notarization gates.

## Conclusion

WP-005 moves forward materially: the documented full gate no longer depends on a
pre-existing local `.venv` or an accidentally compatible `python3`, and the
current committed head passed the full local native/Python/Xcode 27 gate.
Remaining release evidence is still required for live provider E2E, hosted/HF
validation, quality scoring, full manual visual/accessibility review, final
release rollback, and upstream acceptance.
