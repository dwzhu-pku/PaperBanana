# WP-106 Real-Data Search, Selection Cap, And Ten-Reference Dry-Run Evidence

## Metadata

| Item | Value |
|---|---|
| Date | 2026-06-22 07:28 EDT |
| Branch | `integration/native-first-rc-native` |
| Source SHA | `1963b57b42a3` |
| Product-code SHA | `1e77a8c43fc0` |
| Worktree | `/Users/jeff/Codex_projects/PaperBanana-native-integrated` |
| Installed app | `/Applications/PaperBanana.app` |
| Benchmark checkout | `/Users/jeff/Codex_projects/PaperBanana` |
| Run folder | `/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803` |

## Procedure

1. Used the installed Release app already configured with:

   - `settings.repoPath=/Users/jeff/Codex_projects/PaperBanana`
   - `paperbanana.intent.destination=promptStudio`
   - synthetic non-sensitive prompt: `Validate real PaperBananaBench manual reference search/filter and ten-example cap in a no-spend dry run.`

2. Used Accessibility automation against native Prompt Studio, the `workspace-search-search-examples` search field, and exact row labels such as `Reference example ref_1`.
3. Selected `ref_1` through `ref_10` by filtering to each ID and pressing the visible row.
4. Filtered to `ref_11` and attempted to press the row after the cap was reached.
5. Pressed `No-spend dry run`, `Dry Run`, and preflight `Start`.
6. Inspected the generated run artifacts without copying benchmark images or provider payloads into this repository.
7. Ran additional search/filter AX checks for content search, missing-image search, and cleared-search state.

## UI Evidence

| Check | Observed result |
|---|---|
| Ten selected rows | AX proof selected `ref_1`, `ref_2`, `ref_3`, `ref_4`, `ref_5`, `ref_6`, `ref_7`, `ref_8`, `ref_9`, and `ref_10`; each row value changed from `Not selected` to `Selected`. |
| Selected-count cap | After ten selections, visible native texts included `10 reference examples selected`, `10 of 10 selected for prompt enrichment.`, and `10/10`. |
| Eleventh selection blocked | `Reference example ref_11` had `row_11_enabled_before_press=false`; its value before and after pressing began `Selection limit reached. Image available...`. |
| Content search | Searching `GS-RelocNet` produced `1 of 298 shown` with visible row `Reference example ref_4`. |
| Missing-image search | Searching `missing image` produced `3 of 298 shown` with visible rows `ref_71`, `ref_284`, and `ref_309`, each reporting `Image missing`. |
| Cleared search | Clearing the search field produced `298 of 298 shown`. |

## Durable Artifact Checks

The native dry run created:

```text
/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803/events.jsonl
/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803/generated_2K.json
/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803/generated_2K.png
/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803/generated_2K.provider_raw.bin
/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803/generated_2K.provider_response.json
/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803/prompt.txt
/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803/provider_request.json
/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803/request.json
```

Artifact inspection confirmed:

| Artifact | Observed fields |
|---|---|
| `request.json` | `reference_mode=manual_native_prompt_enrichment`, `reference_example_count=10`, and selected IDs exactly `ref_1` through `ref_10`. |
| `request.json` prompt | Contains the `Selected Reference Examples` block and `ID: ref_1` through `ID: ref_10`; it does not contain `ref_11`. |
| `provider_request.json` | `mode=dry_run`, `provider_spend=none`, and provider prompt equal to `request.json` prompt. |
| `generated_2K.json` | `reference_example_count=10` and selected IDs exactly `ref_1` through `ref_10`. |
| UI completion state | AX-visible status included `Completed local dry run without provider spend.` |

The artifact validation command exited 0 and printed:

```json
{
  "run_dir": "/Users/jeff/Codex_projects/PaperBanana/results/native_generate/native_generate_20260622_072803",
  "request_ids": ["ref_1", "ref_2", "ref_3", "ref_4", "ref_5", "ref_6", "ref_7", "ref_8", "ref_9", "ref_10"],
  "metadata_ids": ["ref_1", "ref_2", "ref_3", "ref_4", "ref_5", "ref_6", "ref_7", "ref_8", "ref_9", "ref_10"],
  "provider_mode": "dry_run",
  "provider_spend": "none",
  "metadata_file": "generated_2K.json",
  "request_prompt_contains_selected_block": true,
  "ref_11_in_prompt": false
}
```

## Secret Scan

A targeted scan over `request.json`, `generated_2K.json`, and `provider_request.json` found no `AIza`, `sk-`, `OPENROUTER_API_KEY`, or `GOOGLE_API_KEY` strings.

## Post-Evidence Validation

After staging this evidence update, the aggregate gate was run with the explicit PaperBanana Python environment:

```bash
PYTHONDONTWRITEBYTECODE=1 \
PAPERBANANA_PYTHON=/Users/jeff/Codex_projects/PaperBanana/.venv/bin/python \
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
CODEX_XCODE27_BIN="$(command -v codex-xcode27)" \
  ./script/test_all.sh
```

Result: exit 0. The gate passed native source-control and Xcode project checks, Xcode 27 host audit, `xcodebuild test -test-iterations 3 -retry-tests-on-failure` with 155 Swift tests and 0 failures, Python pytest with 88 tests passing, and `codex-xcode27 proof` with `status=passed halted=False`.

Release install proof was then run:

```bash
DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer \
  ./script/build_and_run.sh --release --install --no-open
```

Result: exit 0. The Release build succeeded and installed `/Applications/PaperBanana.app`.

Material warnings were nonfatal and consistent with earlier native gate output: Xcode reported an empty supported-platforms message for the scheme, and App Intents/linkd registration emitted transient service-connection warnings during unit-test launch.

## Material Warnings

- One preliminary AX script failed to find the dry-run checkbox because SwiftUI exposed `No-spend dry run` as `AXDescription`, not `AXTitle`; the corrected script used `AXDescription` and passed.
- One preliminary search-proof script failed at Swift compile time due a local script type mismatch before interacting with the app; the corrected search proof passed.
- Temporary raw AX proof JSON files were used as local evidence scratch data only and are not intended to be committed.

## Interpretation

This closes the no-spend, real-data native validation slice for Prompt Studio reference search/filter and the 10-example selection cap. The native UI loaded the real local PaperBananaBench diagram metadata, filtered by content and missing-image state, blocked an eleventh selected example at the UI layer, and persisted exactly ten selected references into the provider prompt, `request.json`, and generated metadata while keeping provider spend at `none`.

## Remaining Limitations

- This is still a no-spend dry run; it does not satisfy the approved live provider/fallback native E2E requirement.
- It does not validate broader failure/recovery cases such as provider timeout, malformed response, cancellation, upgrade/rollback, or hosted-session behavior.
- It does not copy benchmark images into run folders, matching the v1 design.
- It does not establish publication-quality output; WP-108 benchmark/rubric evidence remains required before making that claim.
