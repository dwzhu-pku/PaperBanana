# WP-003 Credential Isolation Evidence

Timestamp: 2026-06-22 01:57:57 EDT  
Integration branch: `integration/native-first-rc`  
Integration worktree: `/Users/jeff/Codex_projects/PaperBanana-integration`

## SHAs

| Item | SHA |
|---|---|
| Upstream baseline | `ddeb2a9a8cf6c8119dd29a97c1f1a7312d27dc7b` |
| PR #70 isolated integration commit | `7c41b4223e9074e6c4c1721003aef3b67c86daf6` |
| Current evidence/test commit | `b32617490290d9c77120c425f9bf1200310e7406` |

## Baseline Red Test

Worktree: `/Users/jeff/Codex_projects/PaperBanana-baseline`  
Procedure: temporarily copied
`/Users/jeff/Codex_projects/PaperBanana-pr2-credential-isolation/tests/test_app_credential_isolation.py`
into the detached upstream baseline, ran the focused unittest, then removed the
temporary test file and left the baseline worktree clean.

Command:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. /Users/jeff/Codex_projects/PaperBanana/.venv/bin/python \
  -m unittest tests.test_app_credential_isolation
```

Result: exit 1; 4 tests ran; 3 failed as expected.

Material failures:

- `app.py` assigned `OPENROUTER_API_KEY` and `GOOGLE_API_KEY` into
  `os.environ`.
- `app.py` still contained API-key password textboxes for OpenRouter and Google.
- `app.py` still contained an `apply_keys` callback and `Apply Keys` text.

Interpretation: the test discriminates the unsafe upstream baseline and is not a
non-causal green check.

## Integration Green Tests

PR #70 was cherry-picked as isolated commit `7c41b4223e9074e6c4c1721003aef3b67c86daf6`.
No PR #69, docs, plot, provider, or native code was included in that commit.

Focused command:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. /Users/jeff/Codex_projects/PaperBanana/.venv/bin/python \
  -m unittest tests.test_app_credential_isolation
```

Result on current commit `b32617490290d9c77120c425f9bf1200310e7406`:

```text
Ran 6 tests in 4.022s
OK
```

Full current Python test command:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. /Users/jeff/Codex_projects/PaperBanana/.venv/bin/python \
  -m pytest -q -p no:cacheprovider tests
```

Result:

```text
6 passed in 3.83s
```

Diff hygiene:

```bash
git diff --check
```

Result: exit 0, no output.

## Static Search Classification

Command:

```bash
rg -n 'Apply Keys|type="password"|OPENROUTER_API_KEY|GOOGLE_API_KEY' app.py tests
```

Remaining hits were manually classified:

- `app.py:159` and `app.py:182` are startup/config reads in the refinement
  path.
- `app.py:492-498` computes configured/not-configured status and displays
  non-secret setup guidance.
- `tests/test_app_credential_isolation.py` contains test constants and
  assertions.

No remaining hit is a Gradio API-key password field, `Apply Keys` callback, or
UI-originated write to process-global provider key environment variables.

## Runtime Sentinel Coverage

The credential test suite now includes runtime Gradio config coverage:

- sentinel `OPENROUTER_API_KEY` and `GOOGLE_API_KEY` values are supplied through
  patched environment variables;
- SDK constructors and `shutil.copy2` are patched to avoid client creation and
  generated config-file side effects;
- `app.build_app().get_config_file()` is inspected;
- captured stdout, stderr, and Gradio config text must not include either
  sentinel value;
- the rendered Gradio config must report `OpenRouter: **configured**` and
  `Google Gemini: **configured**`, proving startup configuration is surfaced as
  non-secret status;
- Gradio textbox labels must not include `API Key`, proving the runtime UI
  config does not expose key-entry textboxes.

This is not a full two-client served Gradio session test. The current unresolved
gap is an end-to-end hosted/session proof, which remains required before any
public hosted release claim.

## Worktree State

The disposable baseline worktree was returned to clean detached state after the
temporary red test. The integration worktree was clean after committing this
evidence package except for normal branch-ahead state relative to `origin/main`.

## Limitations

- No hosted Gradio server was launched.
- No browser or Gradio client two-session test was run.
- No live provider credentials were used.
- The current full Python suite on the integration branch contains only the six
  credential-isolation tests because later PR test files are not yet integrated.
